apiVersion: v1
kind: Service
metadata:
  name: {{ cookiecutter.project_slug }}
  labels:
    app: {{ cookiecutter.project_slug }}
spec:
  selector:
    app: {{ cookiecutter.project_slug }}
  ports:
    - name: http
      port: 80
      targetPort: http
