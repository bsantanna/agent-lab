# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
.coverage
coverage.xml
.pytest_cache/
.ruff_cache/

# Environment
.env

# Project artifacts
*.db
tmp/
temp-*
docker-volumes/
{% if cookiecutter.include_aks_gitops %}
# Terraform (tfvars carry per-environment secrets — never commit them)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
{% endif %}
