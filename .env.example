# Auth (Keycloak / Azure AD)
AZURE_CLIENT_ID="???"
AZURE_CLIENT_SECRET="???"
AZURE_TENANT_ID="???"

# App
DEVELOPING="True"
EMBEDDINGS_ENDPOINT="???"
EMBEDDINGS_API_KEY="???"

# Simulations: default LLM executing the agents under test
# (any OpenAI API compatible endpoint, e.g. a LAN inference server)
SIMULATION_LLM_MODEL="???"
SIMULATION_LLM_API_BASE="???"
SIMULATION_LLM_API_KEY="???"

# Simulations: LLM-as-judge (litellm; defaults to openai/gpt-5-nano)
# SIMULATION_JUDGE_MODEL="openai/gpt-5-nano"
# SIMULATION_JUDGE_API_BASE="???"
# SIMULATION_JUDGE_API_KEY="???"

# OpenAI: voice memos simulation (audio chat input) and default judge model
OPENAI_API_KEY="???"

# Observability platforms for simulation experiments
LANGWATCH_API_KEY="???"
LANGWATCH_ENDPOINT="???"
LANGFUSE_HOST="???"
LANGFUSE_PUBLIC_KEY="???"
LANGFUSE_SECRET_KEY="???"
