
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-lab.fullname" . }}
  labels:
    {{- include "agent-lab.labels" . | nindent 4 }}
data:
  EMBEDDINGS_ENDPOINT: "{{ .Values.config.embeddings_endpoint }}"
  OTEL_EXPORTER_OTLP_ENDPOINT: "{{ .Values.config.telemetry_endpoint }}"
