[pytest]
pythonpath = .
addopts = -m "not agent_test"
markers =
    agent_test: end-to-end agent simulations that call live LLM providers and the browser; deselected by default, run explicitly with -m agent_test